## 更新通知（2026-09-08）

- GitHubの最新通常リリースを非同期に確認。起動15秒後・常駐中に確認時期を調べ、前回の確認から24時間未満なら自動通信しない。設定で無効化・手動確認が可能。
- 新版はトレイ通知し、同じバージョンの通知済み情報を設定に保存する。通知・設定・トレイからリリースページへ進める。自動ダウンロードやEXE置換は行わない。
- `v0.2.1` / `0.2.1` 形式を数値比較し、同版・旧版・draft・prerelease・不正タグを除外。ページURLは既知のリポジトリと検証済みタグから構築し、APIが返す任意URLを使わない。
- タイムアウト10秒、応答上限1MiB。通信失敗・API制限・応答不正は更新確認だけを失敗とし、監視を継続。終了時に通信をキャンセルする。`--verify` では自動通信しない。
- 旧settings.jsonのホットキー設定を維持しながら移行し、設定保存は一時ファイルから置換する。更新確認で音声・履歴・プロセス情報を送信しない旨を日英の画面とREADMEへ反映。
- 通常テスト74件成功。新旧バージョン比較、認証情報・本文を送らないリクエスト、404、レート制限、キャンセル、通知重複防止、確認間隔、旧設定の移行・再読込を検証。
- Releaseビルド・単体EXE生成に成功。配布EXEの `--verify` で起動・監視停止・再開を確認し、終了コード0（`artifacts/review/updates-packaged/`）。
- 日本語・英語のWPFプローブで新版がある場合の設定表示、通信失敗時のメッセージ、確認日時の保存、終了処理を検証。実際のトレイ通知表示はWindowsの通知設定に依存する。
- 認証なしの公開API接続はHTTP 404を確認。新版応答はテスト用HTTPハンドラで再現し、公開リリースは作成していない。
- API仕様: [GitHub Releases REST API](https://docs.github.com/en/rest/releases/releases#get-the-latest-release)。テストログ: `artifacts/review/update-tests.txt`。

## 英語・日本語の自動選択（2026-09-08）

- 英語を標準リソース、日本語を `ja` リソースに分離。Windows の表示言語が日本語の場合だけ日本語を選択し、その他は英語にする。数値・日時の地域設定は変更しない。
- メイン画面、設定、詳細、小窓、トレイ、監視状態、エラー説明、履歴の時間・状態表記を対応。保存済み履歴の表示ラベルも現在の言語から計算するため、DB移行は不要。
- 英語の長い文言に合わせ、候補名の省略、説明文・フィルターの折り返し、状態列幅を調整。
- Release ビルドは警告・エラーなし。通常テスト58件に成功（言語選択、ワーカースレッド、全翻訳の存在・書式引数、日本語で保存した履歴の英語表示を含む）。今回、実音の結合テストは再実行していない。
- `en-US`・`ja-JP`・`fr-FR` を指定したUIプローブでメイン、最小サイズ、設定、詳細、小窓を生成。英語・日本語の画像を確認。実際の Windows 表示言語設定を変更する試験は行っていない。
- 単体EXEを再生成し、日本語環境の `--verify` で起動・監視停止・再開を確認、終了コード0。日本語リソースの単体EXEへの同梱も確認。
- 検証出力: `artifacts/review/localization-tests.txt`、`localization-en-US/`、`localization-ja-JP/`、`localization-fr-FR/`、`localization-packaged/`（配布・Git管理対象外）。

## 全体レビューと追加修正（2026-09-08）

対象: Core Audio監視・検出器、候補評価、SQLite、保存ワーカー、WPF本体・小窓・設定・ホットキー、検証コードと配布スクリプトを確認。

### 修正

- [P2] メイン画面はDBの保存待ちで初回表示が止まり、保存キューに残る完了イベントを読み漏らす。小窓と共通のHistorySnapshotから取得し、画面側のCompleted通知・別キャッシュを廃止した。DBロック中でも、未保存100件を含む画面生成が144msで完了することを確認。
- [P2] 履歴削除直前のDispatcher通知が、削除後に実行されて古い音を画面へ戻せる。保存と削除のキュー投入順を同期し、削除対象のキャッシュだけをDB削除成功後に取り除く。DB待ちの間に完了した新しい音は残す。削除を非同期化し、画面と監視スレッドを待たせない。
- [P2] メイン画面のキャッシュは完了順で挿入・件数制限していたため、遅れて終わる長時間音が新しい短音を追い出し得る。開始時刻順の最新10,000件・7日間を共通キャッシュから取得する。履歴の初期ソートも開始時刻の降順にし、後から到着した古い音が最上段に入る問題を修正。
- 保存の防御を補強。無音保持中のSnapshotの終了時刻は統合待ちの仮状態なので、チェックポイントでは終了時刻を保存しない。確定済みの行を未確定チェックポイントが更新しないSQL条件を追加し、統合・確定・遅延保存の順序を回帰試験に追加。

### 検証結果

- Releaseソリューションビルド成功、警告0・エラー0。
- 42チェック成功。実音検出、別プロセスの同時再生、停止・再開、実セッションのミュートと復元を含む。
- 新規回帰試験: 保持期限、10,002件からの最新10,000件選択、両画面の削除反映、確定済み履歴の保護、統合待ちチェックポイント、DBロック中の非同期削除と新しい音の保持。
- WPF確認: 未保存履歴を含む初回表示、DBロック中の画面・小窓生成、ソート維持、追加と値更新、期間・短音フィルター、760×650の履歴表示、WM_HOTKEYによる小窓表示・無効化。
- 自己完結の単体EXEを再生成。隔離DBで起動・停止・再開・終了を確認し終了コード0。配布先とpublish出力のSHA256一致。
- 実行ログ: artifacts/review/full-review-tests.txt、full-review-ui.txt。配布版の検証出力: artifacts/review/full-review-packaged。

### 残る制限

画面表示中の100MB目標は未達。今回の21秒測定は約163MB・CPU約1.53%だが、画像描画と同時の実音テストを含むため安定アイドル性能の数値ではない。Windows 10、物理USB/Bluetooth抜き差し、実ログインによる自動起動、長時間常駐は今回再検証していない。今回、通常ユーザーのDBや自動起動設定は変更していない。

---
# 最新版レビュー（2026-09-08）

## 修正済み（同日）

以下の3件を修正し、単体EXEを更新。

- 直近30秒の候補内で、終了後も30秒以上の音を背景扱いにする。
- 小窓は保存処理と独立したHistorySnapshotを参照する。起動時に履歴とルールを読み込み、完了イベントはDB保存前に反映。無視変更・履歴削除・保持期間もキャッシュへ反映する。
- 音声基盤の生成と通知登録を例外処理内へ移動。失敗したオブジェクトを破棄し、2秒後に再試行。終了操作で待機を中断し、通知登録成功時のみ解除する。

検証: 実音を含む34チェック成功。生成失敗→通知登録失敗→正常復帰を注入試験で確認。再試行中の終了も確認。WPFの小窓はDBロックを保持したまま664msで初回表示（ロック解除を待たないことを検証した値で、通常表示の速度目標ではない）。既存のソート・フィルター・最小サイズ・WM_HOTKEY検証も成功。

以下は修正前のレビュー記録。

更新した配布EXEの隔離DBでの起動・停止・再開・終了確認も成功（終了コード0）。成果物は `artifacts/review/latest-fixed-packaged`。配布EXEとpublish出力のハッシュ一致を確認。

対象: ホットキー・ミュート・履歴フィルター追加後のソース。製品コード・配布EXEは変更していない。

## 1. [P2] 長時間再生が止まると直前の通知音より優先される

場所: `src/AudioCulprit.Core/AudioEvent.cs:71`。

5分の音楽と2秒前の0.2秒の通知を与えると、再生中は通知を選ぶが、音楽に終了時刻を設定するだけで音楽を選ぶ。250msの無音でもDetector.Snapshotが終了扱いになるため、手動停止だけでなく曲間の無音でも起こり得る。長時間音の優先度を下げる条件が「未終了」の場合に限られ、終了直後には終了時刻順で通知を追い越すことが原因。

修正案: 直近30秒という候補範囲を維持し、その範囲内の長時間音は終了後も背景として扱う。古い通知が新しい音を隠さない既存条件も維持する。

## 2. [P2] ホットキーの表示がDBの保存待ちで止まる

場所: `src/AudioCulprit/App.xaml.cs:32`。

ShowQuickはUIスレッド上でRepository.LoadとRulesを同期実行してからウィンドウを生成する。保存専用スレッドと同じRepositoryのロックを使うため、遅い書き込みや整理処理の間、ポップアップとUIが待たされる。保存ロックを1.5秒保持する模擬試験で、同じLoad呼び出しが1504ms停止した。これは通常のDB読み込み時間を計測した値ではない。

修正案: 完了イベントと無視ルールのメモリキャッシュから即表示し、DBアクセスは非同期化する。完了イベントが保存キュー内にある間も参照できる構造にする。

## 3. [P2] 監視スレッドの初期化例外が未処理になる

場所: `src/AudioCulprit.Core/AudioMonitorService.cs:170-171`。

MMDeviceEnumeratorの生成と通知登録は、Run内のtryより前にある。この初期化で例外が発生すると監視ループの再試行処理に入らず、別スレッドなのでApp.OnStartupのcatchにも届かない。バックグラウンドスレッドの未処理例外としてアプリ全体の終了につながる。音声基盤の初期化に失敗する環境で、状態表示や復旧を行えない。

修正案: 初期化から例外処理で囲み、失敗を状態へ反映して再試行する。通知登録が成功した場合だけ解除する。

この項目はコード経路の指摘。Windows Audioサービスや実機の状態を変更した再現試験は実施していない。

## 検証

- 既存テストをReleaseで実行: 18項目成功（今回は実音のintegrationオプションを使用していない）。
- 追加検証: `artifacts/review/latest/LatestReview.csproj`。本番コアクラスで候補逆転と同期読み込み待ちを再現。
- 出力: `Music playing: notice.exe` / `Music stopped: music.exe` / `Popup history-read path blocked: 1504 ms`。
- Windows 10、物理機器の切替、物理キーボードでのホットキー、長期常駐の再検証は実施していない。
- 前回の画面表示時メモリ約161MBという100MB目標未達は残っている。今回の新規計測値ではない。

優先順は候補判定、初期化例外、ポップアップのDB依存解消。
